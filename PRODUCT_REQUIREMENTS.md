# 盯盘 Bar 产品需求文档

> 本文档是盯盘 Bar 的产品功能基准。后续每次新增、修改、删除功能，都需要同步维护本文档。

## 1. 产品概述

**产品名称**：盯盘 Bar

**产品定位**：Mac 菜单栏行情监控工具。用户无需打开交易软件或浏览器，即可在 macOS 顶部菜单栏持续查看自选股票、基金、期货、贵金属等实时行情。

**核心价值**：

- 常驻菜单栏，不进 Dock，不抢占工作流。
- 支持多市场行情统一监控。
- 支持价格提醒、趋势图、刷新状态反馈。
- 面向普通用户提供 DMG 拖拽安装包。

## 2. 目标用户

- 使用 Mac 工作，同时需要轻量关注行情的投资者。
- 同时关注 A 股、港股、美股、基金、贵金属、期货的用户。
- 不希望长期打开重型行情软件，但需要快速知道价格和涨跌状态的用户。

## 3. 当前版本

当前稳定版本：**1.4**

当前安装包：`MacApp/dist/盯盘Bar-1.4.dmg`

macOS 最低版本：**macOS 13.0**

## 4. 支持市场与代码

| 市场 | 示例 | 说明 |
|---|---|---|
| A 股 | `600519`、`000001`、`sh600519` | 支持沪深股票、指数等新浪 A 股行情 |
| 港股 | `00700`、`01024`、`hk00700` | 支持港股五位代码，裸代码自动补齐 |
| 美股 | `AAPL`、`TSLA`、`gb_aapl` | 支持美股代码 |
| 基金 | `f_510300`、`fu_000001`、`of009999` | 支持新浪基金相关 symbol |
| 上金所 | `AU9999`、`AU9995`、`AG9999`、`AUTD`、`AGTD` | 上金所贵金属现货。`AG9999` 是白银，不属于黄金 |
| 期货 | `nf_AU0`、`nf_AG0` | 支持新浪期货 symbol，需保留合约后缀大小写 |

产品 UI 中应使用「上金所」描述 `AU/AG` 现货，避免用「黄金」泛指所有贵金属。

## 5. 菜单栏展示

### 5.1 基础展示

菜单栏每次最多展示一个行情单元，但横向滚动模式下会连续滚动展示所有代码单元。

单个行情单元包含：

- 名称：小字号
- 实时价格
- 涨跌方向：`▲` 或 `▼`
- 涨跌幅：精确到小数点后 1 位，不展示正负号
- 当日/最近交易日分时趋势图
- 交易状态：仅非交易时段展示「休市」

展示格式示意：

```text
名称  价格  ▲1.2%  [趋势图]  休市
```

### 5.2 颜色

状态栏文字颜色必须使用系统自适应颜色，保证深色/浅色菜单栏均可读。

涨跌颜色支持设置：

- 红涨绿跌，默认值
- 红跌绿涨

颜色配置影响：

- 趋势图尾部圆点
- 到价提醒浮层中的方向圆点

颜色配置不影响菜单栏文字颜色。

### 5.3 轮播方式

支持两种展示模式：

- 横向滚动，默认值
- 纵向切换

横向滚动要求：

- 所有监控项连续横向滚动。
- 首尾使用渐入渐出遮罩。
- 两个代码单元之间保留约 3 个英文字符宽度的间隙。
- 使用 Core Animation / CALayer 驱动，避免卡顿。
- 横向滚动时轮播间隔设置不生效。

纵向切换要求：

- 每次展示一个行情单元。
- 按轮播间隔向上滑动切换。
- 轮播间隔范围：1 到 30 秒。

### 5.4 刷新反馈

用户需要知道行情是否仍在正常刷新。

当前设计：

- 每次行情刷新成功时，菜单栏内容执行一次短暂呼吸/脉冲动画。
- 呼吸动画仅在数据刷新成功时发生，不随横向滚动循环反复发生。
- 若超过 2 倍刷新间隔仍未成功刷新，菜单栏整体透明度降低，提示数据可能陈旧。

### 5.5 无数据状态

当前状态：

- 首次启动未拿到数据前显示占位。
- 无有效行情时显示「无数据」。

### 5.6 点击菜单栏后的行情面板

点击菜单栏组件后展示所有已配置的监控项。

每个监控项需要支持二级菜单，二级菜单内展示该标的的数据面板。

数据面板要求：

- 按「基础」「价格」「时间」「市场补充」等分组排版。
- 基础信息包含名称、市场、API 代码、交易状态。
- 价格信息包含最新价、涨跌额、涨跌幅、昨收、今开、最高、最低。
- 如果接口提供成交量、成交额、盘口、52 周高低、市值、盘前/盘后、交易所、品种等字段，应在对应市场补充分组中展示。
- 不展示接口原始字段，避免二级菜单信息过载。

## 6. 分时趋势图

### 6.1 展示位置

分时趋势图展示在涨跌幅之后、交易状态之前。

### 6.2 数据逻辑

- 交易日内展示当日分时趋势。
- 非交易日展示上一个交易日分时趋势。
- 随行情刷新同步刷新。
- 趋势图必须来自真实分钟线数据源。
- 不允许用开盘、最高、最低、现价等 OHLC 快照字段伪造成分时趋势图。
- 若目标市场暂时没有可靠分钟线数据源，则该项不展示趋势图，并在已知问题中记录数据源缺口。

当前分钟线数据源：

| 市场 | 分钟线数据源 | 状态 |
|---|---|---|
| A 股 | 新浪 `CN_MarketDataService.getKLineData` | 已接入 |
| 港股 | 新浪 `HK_MinlineService.getMinline` | 已接入 |
| 美股 | 新浪 `US_MinKService.getMinK` | 已接入；指数需做特殊 symbol 映射，例如 `NDX` → `.NDX` |
| 场内 ETF/LOF 基金 | 按交易所代码转入 A 股分钟线接口，例如 `f_510300` → `sh510300` | 已接入 |
| 上金所 | 东方财富 `push2his` 趋势接口，例如 `118.AU9999` | 已接入 |
| 国内期货 | 新浪 `InnerFuturesNewService.getFewMinLine` | 已接入 |
| 场外基金 | 暂无可靠真实分钟线 | 暂不展示趋势图 |

### 6.3 视觉规则

- 趋势图尾部使用小圆点表示当前相对开盘价涨跌。
- 圆点颜色遵循用户设置的涨跌颜色规则。

## 7. 监控列表

### 7.1 容量

最多支持 30 个监控项。

### 7.2 排序

监控列表不按市场分组展示，统一按添加时间倒序展示。

### 7.3 编辑模式

当前交互：

- 设置页只保留一个总编辑按钮。
- 点击编辑后显示添加、取消、保存。
- 点击添加后，在列表最顶部插入一条空白待输入项。
- 保存后写入监控列表。
- 取消后丢弃本次编辑。

### 7.4 代码输入与检索

输入代码时：

- 延迟 200ms 自动检索。
- 检索结果以浮层候选列表展示。
- 候选最多展示 10 条。
- 点击候选后填入输入框，并锁定该候选对应市场。
- 列表中展示当前代码所属市场。
- 不再展示市场下拉选择。

候选来源：

- 普通股票/基金/期货候选来自新浪搜索建议接口。
- `AU9999` 等上金所现货若搜索建议接口缺失，则通过真实行情接口验证后补充展示。
- 不允许通过拍脑袋维护大规模硬编码别名表。
- 不允许把白银 `AG9999` 错误作为黄金 `AU9999` 的候选。

### 7.5 备注

每个监控项支持填写备注。

若备注为空，状态栏优先展示行情接口返回名称。

## 8. 价格提醒

### 8.1 容量

最多支持 10 条价格提醒。

### 8.2 绑定范围

每条提醒可绑定监控列表中的任意一个代码。

每个代码至多 5 个提醒。

### 8.3 条件

支持：

- 涨到指定价格
- 跌到指定价格

### 8.4 触发频率

支持：

- 仅提醒一次
- 无限次提醒

### 8.5 浮层提示

触发后在菜单栏下方展示黑色背景浮层。

要求：

- 文案居中展示。
- 浮层大小自适应文案。
- 多个提醒同时触发时，使用无序列表符号展示。
- 用小红点/小绿点表示涨跌方向。
- 鼠标 hover 到浮层上时提醒消失。

## 9. 设置页

设置页包含：

- 欢迎文案：`感谢支持，已解锁盯盘 Bar 永久使用权限`
- 一个放烟花的大 Emoji 表情
- 通用设置
- 监控列表
- 价格提醒

通用设置包含：

- 刷新间隔
- 轮播方式
- 纵向轮播间隔
- 涨跌颜色

刷新间隔：

- 最小 5 秒
- 最大 3600 秒
- 默认 60 秒
- 预设：30 秒、1 分钟、2 分钟、5 分钟、10 分钟

## 10. 数据源

主要数据源：

- 新浪行情接口：`https://hq.sinajs.cn/list=...`
- 新浪搜索建议接口：`https://suggest3.sinajs.cn/suggest`

请求要求：

- 行情接口需带 `Referer: https://finance.sina.com.cn`
- 使用合理的 User-Agent
- 需要处理 GB18030 编码

## 11. 持久化

使用 `UserDefaults` 本地存储。

当前 key：

- 监控列表：`WatchedItems.v2`
- 通用设置：`Settings.v2`
- 价格提醒：`PriceAlerts.v1`

要求：

- 不上传用户数据。
- 版本升级不得清空用户已有监控列表。
- 数据结构升级时需做兼容迁移。

## 12. 安装与分发

当前分发方式：

- DMG 安装包
- 拖拽 `DingPanBar.app` 到 Applications

当前限制：

- 使用 ad-hoc 签名。
- 未做 Apple Developer ID 签名与 notarization。
- 用户首次打开可能需要右键打开。

后续建议：

- 使用 Developer ID 正式签名。
- 做 notarization，降低首次打开阻力。

## 13. 非目标

当前阶段不做：

- 交易下单
- 账户登录
- 持仓同步
- K 线完整行情页
- 云端账户体系
- iOS / watchOS 正式版本

## 14. 已知问题与待办

### 已知 Bug

- 部分场外基金暂无可靠真实分钟线数据源，当前不展示趋势图。后续如接入第三方基金分钟线源，需要先验证其是否为真实分钟序列，不能用估值/净值快照补点。

### 1.3 已完成需求

- 全部趋势图改为真实分钟线；移除 OHLC 降级趋势图。
- 补齐上金所、国内期货、美股指数、场内 ETF/LOF 的分钟线数据源。
- 修复纵向切换模式下刷新后重置到第一个监控项的问题。

### 1.4 已完成需求

- 点击菜单栏后，监控列表中的每个标的支持二级菜单数据面板。
- 数据面板按基础、价格、时间和市场补充分组展示可用行情字段。
- 一级菜单不展示「备注」标识，保持列表简洁。
- 二级菜单不展示接口原始字段，避免信息过载。
- 二级菜单文字使用系统正常文字色，提升可读性。

### 文档待同步

- `README.md` 中部分描述仍停留在旧版本，例如监控列表按分组维护、安装方式等，需要后续同步到当前 1.2 行为。

## 15. 维护规则

后续开发要求：

- 每次新增功能，先更新本文档的对应章节。
- 每次修改交互，更新本文档中的规则和边界条件。
- 每次删除功能，在本文档中删除或标记废弃。
- 每次发布版本，在本文档更新当前版本和新增功能。
